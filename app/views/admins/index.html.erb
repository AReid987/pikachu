<h2>Admins</h2>

<%= form_tag admins_path, :method => 'get', :id => 'users_search' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag 'Search', :name => nil %>
  </p>
  <div id='users'><%= render 'admins' %></div>
<% end %>