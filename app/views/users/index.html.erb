<h2 class="h2">Users</h2>

	<%= form_tag users_path, :method => 'get', :class=>"form-group", :id=>"users_search" do %>
    <%= text_field_tag :search, params[:search] %>
 		<button type="submit" class="btn btn-primary">Search</button>
  <div id='users' data-update-url='<%= sort_users_url %>'><%= render 'users' %></div>
<% end %>
